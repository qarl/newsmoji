# Local agent / editor artifacts
.Jimmy/
AGENTS.md
.claude/

# macOS / NFS metadata
._*
.DS_Store

# Runtime state (lives in ~/newsmoji/ on the host, never in the repo)
newsmoji.env
newsmoji.log
cron.err
index.html
feeds.txt

# Python
__pycache__/
*.pyc
